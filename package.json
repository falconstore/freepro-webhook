{
  "name": "freepro-webhook",
  "version": "1.0.0",
  "description": "Webhook para processar pagamentos da LastLink",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
